node_modules/
lib/
*.log
.DS_Store

# 端到端场景随仓库分发,供有运行中 dsh + live 会话时手动跑;
# 不被 vitest 拾取(include 只匹配 *.spec.ts),也不影响构建。

# --- 密钥与本地环境,永不入库 ---
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
secrets.*
credentials.*
**/id_rsa
**/id_ed25519
